# EcoMed AI — зависимости (в корне репозитория для Streamlit Community Cloud).
streamlit>=1.58
pydantic>=2.13
pydantic-settings>=2.14
SQLAlchemy>=2.0
pandas>=2.2
Pillow>=10
python-dotenv>=1.0
rapidfuzz>=3.9

# LLM (OpenAI Agents SDK) — используется для OCR/структурирования и текста отчёта.
# Без ключа приложение работает в ручном режиме, эти пакеты просто не вызываются.
openai>=1.40
openai-agents>=0.17
